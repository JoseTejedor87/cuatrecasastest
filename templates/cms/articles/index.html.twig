{% extends 'cms/base.html.twig' %}
{% trans_default_domain "admin" %}

{% block title %}{{ 'sections.articles.index.title'|trans }}{% endblock %}

{% block body %}
<div class="m-grid__item m-grid__item--fluid m-wrapper">
    <!-- BEGIN: Subheader -->
    <div class="m-subheader ">
        <div class="d-flex align-items-center">
            <div class="mr-auto">
                <h3 class="m-subheader__title m-subheader__title--separator">
                    <i class="m-menu__link-icon flaticon-file"></i>
                    {{ 'sections.articles.index.title'|trans }}
                </h3>
                <ul class="m-subheader__breadcrumbs m-nav m-nav--inline">
                    <li class="m-nav__item">
                        {# <a href="{{ path('articles_new') }}" class="btn btn-label-warning btn-bold btn-sm btn-icon-h">
                            <i class="flaticon-plus"></i> {{ 'global.actions.create'|trans }}
                        </a> #}
                    </li>
                </ul>
            </div>
        </div>
    </div>
    <!-- END: Subheader -->
    <div class="m-content">

        {% if pagination %}
        <div class="m-section">
            <div class="m-portlet">
                <table class="table table-hover">
                    <thead class="thead-light">
                        <tr>
                            <th>{{ knp_pagination_sortable(pagination, 'entities.article.fields.title'|trans, 'title') }}</th>
                            <th>{{ knp_pagination_sortable(pagination, 'entities.article.fields.publication_date'|trans, 'publication_date') }}</th>
                            <th colspan="2" style="width:10%;"></th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for article in pagination %}
                        <tr>
                            <td>{{ article.translate(app.request.getLocale()).title ?  article.translate(app.request.getLocale()).title : article.translate('en').title}}</td>
                            <td>{{ article.publicationdate ? article.publicationdate|date('d-m-Y') : ''  }}</td>
                            <td class="text-nowrap">
                                {% include 'cms/tools/languageStatus.html.twig' with {'values':article.languages} %}
                            </td>
                            <td>
                                {# <a class="btn btn-primary btn-sm m-btn m-btn--icon m-btn--icon-only m-btn--pill" href="{{ path('articles_edit', {'id': article.id}) }}">
                                    <i class="fa fa-edit"></i>
                                </a> #}
                            </td>
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>

        <div class="m-section">
            {{ knp_pagination_render(pagination) }}
        </div>

        {% else %}
        <div class="row">
            <div class="col">
                <div class="alert alert-elevate fade show" role="alert">
                    {{ 'global.messages.noRecordsFound'|trans }}
                </div>
            </div>
        </div>
        {% endif %}
    </div>

</div>
{% endblock %}





{# <!DOCTYPE html>

<title>Articles index</title>

{% block body %}
    <h1>Articles index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>CreatedAt</th>
                <th>UpdatedAt</th>
                <th>Languages</th>
                <th>Locations</th>
                <th>Status</th>
                <th>Featured</th>
                <th>Id</th>
                <th>OldId</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for article in articles %}
            <tr>
                <td>{{ article.createdAt ? article.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ article.updatedAt ? article.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ article.languages ? article.languages|json_encode : '' }}</td>
                <td>{{ article.locations ? article.locations|json_encode : '' }}</td>
                <td>{{ article.status }}</td>
                <td>{{ article.featured }}</td>
                <td>{{ article.id }}</td>
                <td>{{ article.oldId }}</td>
                <td>
                    <a href="{{ path('articles_show', {'id': article.id}) }}">show</a>
                    <a href="{{ path('articles_edit', {'id': article.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="9">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('articles_new') }}">Create new</a>
{% endblock %} #}
