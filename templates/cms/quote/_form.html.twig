{% trans_default_domain "admin" %}

<div class="row">
    <div class="col-md-12">
        {{ form_row(form.author) }}
        {{ form_row(form.year) }}
        {{ form_row(form.translations) }}
    </div>
</div>

{{ form_rest(form) }}