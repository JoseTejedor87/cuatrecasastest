{% extends 'web/base.html.twig' %}

{% block stylesheets %}
<!-- Slider CSS -->
<link href="{{ asset('web/vendor/swiper/css/swiper.min.css') }}" rel="stylesheet">
{% endblock %}

{% block main %}

    <main role="main">
        <article class="container">

            <header class="row page__header page__header--opacity" style="background-image: url('{{ asset('web/assets/img/andrej-lisakov-7G8ARu_DJTc-unsplash.jpg') }}')">
                <div class="col">
                    <div class="info__block">
                        <h1 class="page__title">
                            {{ 'sections.knowledge.title' | trans }}
                        </h1>
                        <nav aria-label="breadcrumb">
                            <ol class="breadcrumb">
                                <li class="breadcrumb-item">
                                    <a href="{{ path('home') }}">{{ 'global.home'|trans }}</a>
                                </li>
                                <li class="breadcrumb-item active" aria-current="page">
                                    {{ 'sections.knowledge.title' | trans }}
                                </li>
                            </ol>
                        </nav>
                    </div>
                    <ul class="action__links">
                        <li><a href="#"><i class="material-icons">share</i></a></li>
                        <li><a href="#"><i class="material-icons">print</i></a></li>
                        <li><a href="#"><i class="material-icons">bookmark_border</i></a></li>
                    </ul>
                </div>
            </header>

            <section class="row page__section search_box">
                <div class="col search_box__wrapper">
                    <div class="search_box__name search_box__name--full">
                        <form action="" method="get">
                            <input type="text" class="form-control" placeholder="Buscar" name="initial">
                            <div class="input-group">
                                <button class="search_box__button" type="submit">
                                    <span class="icon4-lupa"></span>
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </section>

            <!-- FILTERS -->
            <section id="filterWrapper" class="row">
                <div class="col-12">
                    <!-- Accordion filters -->
                    <div id="accordionFilters">
                        <!-- Group selector -->
                        <ul id="filterTabs" class="nav nav-pills nav-fill" role="tablist">
                            <li class="nav-item">
                                <a class="collapsed nav-link" role="button" data-toggle="collapse" data-target="#collapse01" aria-expanded="false" aria-controls="collapse01">Sector</a>
                            </li>
                            <li class="nav-item">
                                <a class="collapsed nav-link" role="button" data-toggle="collapse" data-target="#collapse02" aria-expanded="false" aria-controls="collapse02">Servicios</a>
                            </li>
                            <li class="nav-item">
                                <a class="collapsed nav-link" role="button" data-toggle="collapse" data-target="#collapse03" aria-expanded="false" aria-controls="collapse03">Tipo</a>
                            </li>
                            <li class="nav-item">
                                <a class="collapsed nav-link" role="button" data-toggle="collapse" data-target="#collapse04" aria-expanded="false" aria-controls="collapse04">Región</a>
                            </li>
                            <li class="nav-item">
                                <a class="collapsed nav-link" role="button" data-toggle="collapse" data-target="#collapse05" aria-expanded="false" aria-controls="collapse05">Fecha</a>
                            </li>
                            <li class="nav-item">
                                <a class="collapsed nav-link" role="button" data-toggle="collapse" data-target="#collapse06" aria-expanded="false" aria-controls="collapse06">Colección</a>
                            </li>
                            <li class="nav-item">
                                <a class="collapsed nav-link" role="button" data-toggle="collapse" data-target="#collapse07" aria-expanded="false" aria-controls="collapse07">Formato</a>
                            </li>
                        </ul>
                        <!-- Checkboxes list -->
                        <div id="filterTabsContent">
                            <div id="collapse01" class="collapse" aria-labelledby="heading01" data-parent="#accordionFilters">
                                <div class="filters__order">
                                    <span>Ordenar por: </span>
                                    <ul>
                                        <li><a class="order_abc" href="#">orden alfabético</a></li>
                                        <li><a class="order_pop" href="#">popular</a></li>
                                    </ul>
                                </div>
                                <div class="chechbox__wrapper">
                                {% for sector in sectors %}
                                    {% if sector.translate(app.request.getLocale()).title  %}
                                        <div class="checkbox">
                                            <input type="checkbox" class="custom-control-input" id="sector-{{sector.id}}" data-name="{{ sector.translate(app.request.getLocale()).title|raw  }}" value="{{sector.id}}">
                                            <label class="checkbox-label" for="sector-{{sector.id}}"><span>{{ sector.translate(app.request.getLocale()).title|raw  }}</span></label>
                                        </div>
                                    {% endif %}
                                {% endfor %}
                                </div>
                                <div class="filters__apply">
                                    <button type="button" class="btn btn__naked apply_button">Aplicar</button>
                                </div>
                            </div>
                            <div id="collapse02" class="collapse" aria-labelledby="heading02" data-parent="#accordionFilters">
                                <div class="filters__order">
                                    <span>Ordenar por: </span>
                                    <ul>
                                        <li><a class="order_abc" href="#">orden alfabético</a></li>
                                        <li><a class="order_pop" href="#">popular</a></li>
                                    </ul>
                                </div>
                                <div class="chechbox__wrapper">
                                    {% for practice in practices %}
                                        {% if practice.translate(app.request.getLocale()).title  %}
                                            <div class="checkbox">
                                                <input type="checkbox" class="custom-control-input" id="services-{{practice.id}}" data-name="{{ practice.translate(app.request.getLocale()).title|raw  }}" value="{{practice.id}}">
                                                <label class="checkbox-label" for="services-{{practice.id}}"><span>{{ practice.translate(app.request.getLocale()).title|raw  }}</span></label>
                                            </div>
                                        {% endif %}
                                    {% endfor %}
                                </div>
                                <div class="filters__apply">
                                    <button type="button" class="btn btn__naked apply_button">Aplicar</button>
                                </div>
                            </div>
                            <div id="collapse03" class="collapse" aria-labelledby="heading03" data-parent="#accordionFilters">
                                <div class="filters__order">
                                    <span>Ordenar por: </span>
                                    <ul>
                                        <li><a class="order_abc" href="#">orden alfabético</a></li>
                                        <li><a class="order_pop" href="#">popular</a></li>
                                    </ul>
                                </div>
                                <div class="chechbox__wrapper">
                                    {% for type in types %}
                                        {% if type  %}
                                        {% set typeTrans = 'sections.knowledge.knowledgeTypes.'~type %}
                                            <div class="checkbox">
                                                <input type="checkbox" class="custom-control-input" id="type-{{type}}" data-name="{{ typeTrans | trans }}" value="{{ type | trans }}">
                                                <label class="checkbox-label" for="type-{{type}}"><span>{{ typeTrans | trans }}</span></label>
                                            </div>
                                        {% endif %}
                                    {% endfor %}
                                </div>
                                <div class="filters__apply">
                                    <button type="button" class="btn btn__naked apply_button">Aplicar</button>
                                </div>
                            </div>
                            <div id="collapse04" class="collapse" aria-labelledby="heading04" data-parent="#accordionFilters">
                                <div class="filters__order">
                                    <span>Ordenar por: </span>
                                    <ul>
                                        <li><a class="order_abc" href="#">orden alfabético</a></li>
                                        <li><a class="order_pop" href="#">popular</a></li>
                                    </ul>
                                </div>
                                <div class="chechbox__wrapper">
                                    {% for office in offices %}
                                        {% if office.city  %}
                                            <div class="checkbox">
                                                <input type="checkbox" class="custom-control-input" id="region-{{office.id}}" data-name="{{ office.city }}" value="{{office.id}}">
                                                <label class="checkbox-label" for="region-{{office.id}}"><span>{{ office.city  }}</span></label>
                                            </div>
                                        {% endif %}
                                    {% endfor %}
                                </div>
                                <div class="filters__apply">
                                    <button type="button" class="btn btn__naked apply_button">Aplicar</button>
                                </div>
                            </div>
                            <div id="collapse05" class="collapse" aria-labelledby="heading05" data-parent="#accordionFilters">
                                <div class="filters__order">
                                    <span>Ordenar por: </span>
                                    <ul>
                                        <li><a class="order_abc" href="#">orden alfabético</a></li>
                                        <li><a class="order_pop" href="#">popular</a></li>
                                    </ul>
                                </div>
                                <div class="chechbox__wrapper">
                                    {% for i in 2010..2020 %}
                                         <div class="checkbox">
                                            <input type="checkbox" class="custom-control-input" id="date-{{ i }}" data-name="{{ i }}" value="{{ i }}">
                                            <label class="checkbox-label" for="date-{{ i }}"><span>{{ i }}</span></label>
                                        </div>
                                         
                                    {% endfor %}
                                </div>
                                <div class="filters__apply">
                                    <button type="button" class="btn btn__naked apply_button">Aplicar</button>
                                </div>
                            </div>
                            <div id="collapse06" class="collapse" aria-labelledby="heading06" data-parent="#accordionFilters">
                                <div class="filters__order">
                                    <span>Ordenar por: </span>
                                    <ul>
                                        <li><a class="order_abc" href="#">orden alfabético</a></li>
                                        <li><a class="order_pop" href="#">popular</a></li>
                                    </ul>
                                </div>
                                <div class="chechbox__wrapper">
                                    <div class="checkbox">
                                        <input type="checkbox" class="custom-control-input" id="collection-01" value=".collection-01">
                                        <label class="checkbox-label" for="collection-01"><span>Collection 01</span></label>
                                    </div>
                                    <div class="checkbox">
                                        <input type="checkbox" class="custom-control-input" id="collection-02" value=".collection-02">
                                        <label class="checkbox-label" for="collection-02"><span>Collection 02</span></label>
                                    </div>
                                    <div class="checkbox">
                                        <input type="checkbox" class="custom-control-input" id="collection-03" value=".collection-03">
                                        <label class="checkbox-label" for="collection-03"><span>Collection 03</span></label>
                                    </div>
                                    <div class="checkbox">
                                        <input type="checkbox" class="custom-control-input" id="collection-04" value=".collection-04">
                                        <label class="checkbox-label" for="collection-04"><span>Collection 04</span></label>
                                    </div>
                                    <div class="checkbox">
                                        <input type="checkbox" class="custom-control-input" id="collection-05" value=".collection-05">
                                        <label class="checkbox-label" for="collection-05"><span>Collection 05</span></label>
                                    </div>
                                    <div class="checkbox">
                                        <input type="checkbox" class="custom-control-input" id="collection-06" data-name="" value=".collection-06">
                                        <label class="checkbox-label" for="collection-06"><span>Collection 06</span></label>
                                    </div>
                                </div>
                                <div class="filters__apply">
                                    <button type="button" class="btn btn__naked apply_button">Aplicar</button>
                                </div>
                            </div>
                            <div id="collapse07" class="collapse" aria-labelledby="heading07" data-parent="#accordionFilters">
                                <div class="filters__order">
                                    <span>Ordenar por: </span>
                                    <ul>
                                        <li><a class="order_abc" href="#">orden alfabético</a></li>
                                        <li><a class="order_pop" href="#">popular</a></li>
                                    </ul>
                                </div>
                                <div class="chechbox__wrapper">
                                    {% for format in formats %}
                                        {% if format  %}
                                        {% set formatTrans = 'sections.knowledge.knowledgeFormat.'~format %}
                                            <div class="checkbox">
                                                <input type="checkbox" class="custom-control-input" id="format-{{format}}" data-name="{{ formatTrans | trans }}" value="{{ format | trans }}">
                                                <label class="checkbox-label" for="format-{{format}}"><span>{{ formatTrans | trans }}</span></label>
                                            </div>
                                        {% endif %}
                                    {% endfor %}                 
                                </div>
                                <div class="filters__apply">
                                    <button type="button" class="btn btn__naked apply_button">Aplicar</button>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Selected tags -->
                <div class="col-12">
                    <div id="checksOutput" style="display:none"></div>
                </div>
            </section>

            <!-- MASONRY GRID JS -->
            <section class="row page__section page__section--fullbg">
                <div class="col-12">
                    <h4 class="section__title section__title--recomended">Te recomendamos</h4>
                </div>
                <div class="col-12">
                    <div class="grid__masonry">
                        <div class="grid__sizer"></div>
                        <div class="grid__gutter"></div>
                        {% for key,publication in publications %}
                            {% set style = ["grid__item--dobleW" , "grid__item--normal","grid__item--dobleH", "grid__item--normal", "grid__item--normal","grid__item--dobleH", "grid__item--dobleW", "grid__item--normal","grid__item--dobleH", "grid__item--normal", "grid__item--normal","grid__item--dobleH", "grid__item--dobleW", "grid__item--normal"] %}
                            {# {% set fecha = publication.publication_date |format_datetime('long', 'none',locale='es')|split(' ') %} #}
                            <div class="grid__item {{ style[key] }}">
                                <article class="boxnews__item boxnews__item--{{ publication.type  }}">
                                    <div class="boxnews__header">
                                        <div class="boxnews__type">{{ publication.type  }}
                                        {# Opinion #}
                                        </div>
                                        <div class="boxnews__info">
                                        {{ publication.fechaPubli }} 
                                        {# 3 Marzo 2020 #}
                                        </div>
                                        <div class="boxnews__button">
                                            <button type="button" class="button__bookmark"></button>
                                        </div>
                                    </div>
                                    <div class="boxnews__content">
                                        <h5 class="boxnews__copy">
                                            <a href="articulo/{{ publication.translate(app.request.getLocale()).slug|raw  }}" class="boxnews__link">
                                                {{ publication.translate(app.request.getLocale()).title|raw  }}
                                            </a>
                                        </h5>
                                        <div class="boxnews__img" style="background-image: url('{{ publication.photo  }}')"></div>
                                    </div>
                                </article>
                            </div>
                        {% endfor %}  
                    </div>
                </div>
                {% if page < pagesTotal %}
                <div class="col-12">
                    <div class="viewmore__button">
                        <button type="button" class="btn btn__viewmore" data-page="2">Cargar más</button>
                    </div>
                </div>
                {% endif %}
            </section>

        </article>
    </main>

    {% include 'web/shared/related-events-slider.html.twig' %}
    {% include 'web/shared/related-legal-slider.html.twig' %}
    {% include 'web/shared/collections.html.twig' with {insights: []} %}
    {% include 'web/shared/call-to-action.html.twig' %}

{% endblock %}

{% block javascripts %}
<!-- Slider JS -->
<script type="text/javascript" src="{{ asset('web/vendor/swiper/js/swiper.min.js') }}"></script>
<!-- Checkbox Filters -->
<script type="text/javascript" src="{{ asset('web/assets/js/checkboxFilters.js') }}"></script>
<!-- Masonry JS -->
<script type="text/javascript" src="{{ asset('web/vendor/masonry.pkgd.min.js') }}"></script>

<script>
    $(function () {
        // GRID
        var masonryOptions = {
            itemSelector: '.grid__item',
            columnWidth: '.grid__sizer',
            gutter: '.grid__gutter',
            percentPosition: true
        };
        var $grid = $('.grid__masonry').masonry( masonryOptions );
        $grid.masonry( masonryOptions );
        // SLIDER
        var swiperGeneral = new Swiper ('.slider__general', {
            slidesPerView: 3,
            spaceBetween: 25,
            allowTouchMove: true,
            grabCursor: true,
            scrollbar: {
                el: '.swiper-scrollbar',
                draggable: true
            }
        });
        $(document).on('click', '.btn__viewmore', function(event){
            var page =  $(this).data("page");
            ajax(page);
            
        });
        $(document).on('click', '.apply_button', function(e){
            ajax();
        });
        $(document).on('click', '#deleteFilters', function(e){
            $("input:checkbox:checked").each(function() {
                $(this).prop('checked', false);
            });
            $('#checksOutput').empty();
            ajax();
        });
        var $checksOutput = $('#checksOutput');
        $($checksOutput).on('click', '.checkbox-tag .close-tag', function(element){
            var value = $(this).data( "value" );
                $checkBoxes.each(function(i, elem) {
                    if($(elem).attr("data-name") == value){
                        $(elem).prop( "checked", false );
                    }

                });
                ajax();
        });
        function ajax(page) {
            var sectors = new Array();
            var services = new Array();
            var type = new Array();
            var region = new Array();
            var date = new Array();
            var format = new Array();
            var page = page ? page : $(this).data("page");
            var item = "";
            var style = ["grid__item--dobleW" , "grid__item--normal","grid__item--dobleH", "grid__item--normal", "grid__item--normal","grid__item--dobleH", "grid__item--dobleW", "grid__item--normal","grid__item--dobleH", "grid__item--normal", "grid__item--normal","grid__item--dobleH", "grid__item--dobleW", "grid__item--normal"];
            $("input:checkbox:checked").each(function() {
                var result = $(this)[0].id.split('-');
                if(result[0] == "sector"){
                    sectors.push($(this).val());
                }
                if(result[0] == "services"){
                    services.push($(this).val());
                }
                if(result[0] == "type"){
                    type.push($(this).val());
                }
                if(result[0] == "region"){
                    region.push($(this).val());
                }
                if(result[0] == "date"){
                    date.push($(this).val());
                }
                if(result[0] == "format"){
                    format.push($(this).val());
                }
            });
 
            if(!page){
                page=1;
            }
            item = $(".grid__item").first().clone();
            
            var html = '';
            $.ajax({
                method: "GET",
                data: { sector: sectors.toString(), services: services.toString(), type: type.toString(),office: region.toString(),date: date,format: format.toString(), page :page   },
                url: "{{ path('knowledge_index') }}",
                beforeSend: function( xhr ) {
                    
                }
            })
            .done(function( msg ) {
                var url="";
                if(msg['office']){
                    if(url == ""){
                        url= "?office="+msg['office'];
                    }else{
                        url= url + "&office="+msg['office'];
                    }
                }
                if(msg['textSearch']){
                    if(url == ""){
                        url= "?textSearch="+msg['textSearch'];
                    }else{
                        url= url + "&textSearch="+msg['textSearch'];
                    }
                }
                if(msg['services']){
                    if(url == ""){
                        url= "?services="+msg['services'];
                    }else{
                        url= url + "&services="+msg['services'];
                    }
                }
                if(msg['sector']){
                    if(url == ""){
                        url= "?sector="+msg['sector'];
                    }else{
                        url= url + "&sector="+msg['sector'];
                    }
                }
                if(msg['type']){
                    if(url == ""){
                        url= "?type="+msg['type'];
                    }else{
                        url= url + "&type="+msg['type'];
                    }
                }
                if(msg['date']){
                    if(url == ""){
                        url= "?date="+msg['date'];
                    }else{
                        url= url + "&date="+msg['date'];
                    }
                }
                if(msg['format']){
                    if(url == ""){
                        url= "?format="+msg['format'];
                    }else{
                        url= url + "&format="+msg['format'];
                    }
                }
                if(msg['page']>1){
                    if(url == ""){
                        url= "?page="+msg['page'];
                    }else{
                        url= url + "&page="+msg['page'];
                    }
                }
                if(msg['page'] == 1){
                    $('.grid__item').remove();
                }
                $grid.masonry('reloadItems');
                $grid.masonry('destroy');
                msg['publications'].forEach(function(publication, i){
                    item.find( "a" ).text(publication['title']);
                    item.find( ".boxnews__info" ).text(publication['Publication_date']);
                    item.find( ".boxnews__type" ).text(publication['publicationType']);
                    item.find( ".boxnews__img" ).css("background-image", "url("+publication['photo']+")");
                    
                    item.find( ".boxnews__item--article" ).removeClass("boxnews__item--article");
                    item.find( ".boxnews__item" ).addClass('boxnews__item--'+publication['publicationType'])
                    var html = '<div class="grid__item '+style[i]+'">'+item.html()+ '<div>';
                    $grid.append(html);
                });
                $grid.masonry( masonryOptions ); 
                //
                $('.btn__viewmore').data("page",parseInt(msg['page'])+1);
                window.history.replaceState(null, null, "conocimiento"+url);
                console.log(parseInt(msg['page']));
                console.log(parseInt(msg['pagesTotal']));
                if(parseInt(msg['page']) > parseInt(msg['pagesTotal'])){
                    $('.btn__viewmore').hide();
                }else{
                    $('.btn__viewmore').show();
                }
            });
        }
    });
</script>
{% endblock %}
